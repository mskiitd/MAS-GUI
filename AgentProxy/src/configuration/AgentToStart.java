package configuration;

/**
 * @author Anand Prajapati
 *
 * Enum class to take input about what all agents to start when MAS is started
 */
public enum AgentToStart {
	GSA,
	Machine,
	customer,
	All
}
